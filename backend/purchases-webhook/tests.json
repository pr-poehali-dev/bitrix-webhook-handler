{
  "tests": [
    {
      "name": "OPTIONS request returns CORS headers",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200,
      "expectedBody": {},
      "bodyMatcher": "partial"
    },
    {
      "name": "POST without deal_id returns error",
      "method": "POST",
      "path": "/",
      "body": {},
      "expectedStatus": 400,
      "expectedBody": {
        "success": false,
        "error": "string"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "POST with deal_id creates webhook log",
      "method": "POST",
      "path": "/",
      "body": {
        "deal_id": "12345",
        "event": "OnCrmDealUpdate"
      },
      "expectedStatus": 200,
      "expectedBody": {
        "success": true,
        "webhook_id": "number",
        "deal_id": "string"
      },
      "bodyMatcher": "partial"
    }
  ]
}
